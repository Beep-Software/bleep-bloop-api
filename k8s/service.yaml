apiVersion: v1
kind: Service
metadata:
  name: bleep-bloop-api-service
  namespace: bleep-api
spec:
  type: NodePort
  selector:
    app: bleep-bloop-api
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000